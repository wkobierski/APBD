using cwiczenia2.Application;

var app =  new App();
app.Run();


//TODO: 1. functionalities, 2. business logic, 2.5 add an enum for availability statuses, 3. saving to/reading from a JSON file,
// 4. refactor, 5. check against the requirements, 6. (optionally) filtering reports